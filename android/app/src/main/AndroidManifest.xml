<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:fullBackupOnly="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- ML Kit Barcode-Scanning (Block F Phase 4): benoetigtes Modell fuer
             die Barcode-Erkennung, wird bei Bedarf von Google Play Services
             nachgeladen. Siehe @capacitor-mlkit/barcode-scanning README. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Kamera ist nur fuer den optionalen Barcode-Scan noetig (Lebensmittel
         lassen sich auch manuell suchen) - ohne dieses Tag wuerde Android aus
         der CAMERA-Berechtigung automatisch eine PFLICHT-Hardwareanforderung
         ableiten und alle Geraete ohne Kamera (z.B. manche Tablets) komplett
         aus dem Play Store filtern. Fund aus Play-Console-Detailansicht,
         26.07.2026. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Kamera fuer Barcode-Scan von Lebensmittelverpackungen (Block F Phase 4).
         Wird ausschliesslich bei expliziter Nutzer-Aktion (Button "Barcode
         scannen") angefragt, nie automatisch. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Benachrichtigungen (Android 13+ / API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Geplante Notifications nach Gerät-Reboot wieder starten -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Hinweis: KEINE SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM Permissions.
         Google Play erlaubt exakte Alarme nur fuer Wecker- oder Kalender-Apps.
         Fuer Gesundheits-Reminder reicht der Standard-Inexact-Alarm (+/- Minuten),
         den @capacitor/local-notifications automatisch nutzt, wenn diese
         Permissions fehlen. -->

    <!-- Vibrations-Feedback bei Notifications -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- In-App Purchases (Google Play Billing) -->
    <uses-permission android:name="com.android.vending.BILLING" />
</manifest>

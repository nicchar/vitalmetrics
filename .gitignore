# ── Abhängigkeiten ────────────────────────────────────────────────────────────
node_modules/

# ── Android Build-Artefakte ──────────────────────────────────────────────────
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/capacitor-cordova-android-plugins/build/
*.apk
*.aab
*.keystore
*.jks

# ── Release-Build-Ausgabe (wird per build:release erzeugt, kein Quellcode) ────
dist-release/

# ── Microsoft-Office-Sperrdateien (entstehen beim Öffnen von .docx/.xlsx) ─────
~$*

# ── Capacitor ────────────────────────────────────────────────────────────────
# capacitor.config.json NICHT ignorieren – gehört ins Repo

# ── Umgebungs- und Geheimnis-Dateien ─────────────────────────────────────────
.env
.env.*
google-services.json
keystore-base64.txt
android/keystore.properties
# Hinweis: google-services.json nur ins Repo wenn es KEINE API-Keys enthält.
# Für VitalMetrics ohne Firebase kann die Datei ignoriert werden.
# keystore.properties.example (OHNE echte Passwörter) darf ins Repo.

# ── IDE und Betriebssystem ────────────────────────────────────────────────────
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
desktop.ini

# ── Logs und temporäre Dateien ────────────────────────────────────────────────
*.log
npm-debug.log*
.npm/

# ── Cowork (Claude Desktop) ───────────────────────────────────────────────────
.skills/
*.skill
